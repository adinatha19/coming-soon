name: Build and Deploy to Hostinger VPS

on:
  push:
    branches:
      - main  # Deploy when changes are pushed to main branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'  # Or whatever version your project uses

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: List build files (for debugging)
        run: ls -la build  # Or dist, if your build folder is named differently

      - name: Clean target directory on VPS (optional but recommended)
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          passphrase: ${{ secrets.VPS_PASSPHRASE }}
          script: |
            sudo rm -rf /var/www/html/*
            sudo mkdir -p /var/www/html
            sudo chown $USER:$USER /var/www/html

      - name: Upload build folder contents to VPS
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          passphrase: ${{ secrets.VPS_PASSPHRASE }}
          source: "build/*"
          target: "/var/www/html"
          overwrite: true
          
      - name: Restart NGINX (optional)
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          passphrase: ${{ secrets.VPS_PASSPHRASE }}
          script: |
            sudo systemctl restart nginx
